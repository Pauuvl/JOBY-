from django.apps import AppConfig


class StreaksConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.streaks'
    label = 'streaks'
    verbose_name = 'Streaks & Gamification'
